# C++ 课程智能体系统 — 课程设计报告

**大连理工大学 · 软件学院**

---

## 1 引言

### 1.1 研究背景

C++ 作为计算机科学与软件工程专业的核心课程，是学生掌握面向对象编程思想的重要基础。传统的 C++ 教学模式主要依赖课堂讲授与纸质作业，存在以下突出问题：

- **答疑时效性差**：学生在课后遇到问题时无法及时获得解答，依赖教师邮件或办公时间回复，反馈周期长。
- **考核形式单一**：考核以笔试为主，难以全面评估学生的编程实践能力和概念理解水平。
- **学习资源分散**：课件、教材、参考资料分散在不同的平台，学生难以快速定位与当前知识点相关的内容。
- **缺乏个性化指导**：大班教学场景下，教师难以针对每位学生的薄弱环节提供精准辅导。

近年来，大语言模型（Large Language Model, LLM）技术的快速发展为教育领域带来了新的可能性。通过将 LLM 与课程知识库相结合，构建 RAG（Retrieval-Augmented Generation）增强的教学辅助系统，可以有效解决上述问题。

### 1.2 项目目标

本项目旨在设计并实现一套基于大语言模型的 C++ 课程智能体系统，核心目标包括：

1. **构建课程专属知识库**：支持教师上传课件资料（PDF、PPT、Word、Markdown 等格式），自动解析文本内容并建立可检索的知识索引。
2. **实现 AI 智能答疑**：学生通过对话界面提出问题，系统基于课程知识库检索相关内容，结合大语言模型生成专业、准确的回答。
3. **自动化考核评估**：教师配置章节考核参数，系统调用 LLM 自动生成题目，学生在线作答后自动批改并生成多维评价报告。
4. **学习数据追踪**：记录学生的考试成绩、错题信息、对话历史，生成学习仪表盘，辅助教师掌握学情。

---

## 2 国内外研究/技术现状

### 2.1 智能教学系统

#### 2.1.1 国外研究现状

国外智能教学系统（Intelligent Tutoring System, ITS）的研究起步较早。Carnegie Mellon University 开发的 Cognitive Tutor 系统将认知模型与教学内容相结合，在数学教育领域取得了显著效果。Stanford University 的 Code In Place 项目利用在线平台为大规模学生群体提供编程教学。

在 LLM 应用于教育方面，Khan Academy 推出的 Khanmigo 基于 GPT-4 构建，提供个性化的学习辅导和即时反馈。Duolingo 的 Duolingo Max 利用 GPT-4 实现了角色扮演对话和答案解释功能。

#### 2.1.2 国内研究现状

国内智能教育领域发展迅速。清华大学研发的"智海"系列教育大模型专注于计算机学科教学。各大在线教育平台（如中国大学 MOOC、学堂在线）也开始引入 AI 助教功能，但大多停留在简单的问答匹配阶段，缺乏与课程知识库的深度整合。

### 2.2 RAG 与大模型应用

#### 2.2.1 RAG 技术概述

RAG（Retrieval-Augmented Generation）由 Lewis 等人于 2020 年提出，其核心思想是在 LLM 生成回答之前，先从外部知识库中检索相关文档片段，将其作为上下文注入到提示词中，从而减少模型的幻觉问题，提高回答的事实准确性。RAG 在垂直领域知识问答、企业知识库、学术研究辅助等场景中得到了广泛应用。

#### 2.2.2 技术选型分析

本系统采用的技术栈包括：

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 后端框架 | FastAPI | 原生支持异步，性能优异，自动生成 API 文档 |
| 数据库 | SQLite + SQLAlchemy | 轻量级、零配置，适合课程级应用 |
| 前端框架 | Vue 3 + Vite | 组件化开发，热更新，开发效率高 |
| UI 组件库 | Element Plus | 开箱即用，中文友好，组件丰富 |
| 大模型 | DeepSeek API | 兼容 OpenAI 接口，性价比高 |
| 认证 | JWT + bcrypt | 无状态认证，安全性高 |
| 部署 | Docker + Nginx | 一键部署，环境隔离 |

---

## 3 需求与约束分析

### 3.1 功能需求

#### 3.1.1 学生端需求

| 编号 | 功能 | 描述 |
|------|------|------|
| S-01 | 注册/登录 | 学生注册账号并登录系统 |
| S-02 | 选择教师 | 学生选择一名教师，获取该教师的课程资源 |
| S-03 | AI 对话 | 基于 RAG 的智能答疑，流式输出，附带资源推荐和追问提示 |
| S-04 | 资料浏览 | 查看教师上传的课件，支持 PDF/图片/文本在线预览 |
| S-05 | 章节考核 | 参加教师配置的考试，支持选择题、判断题、简答题、编程题 |
| S-06 | 评价报告 | 查看考试成绩和多维度能力评价，导出 HTML 报告 |
| S-07 | 错题本 | 自动收集错题，支持移除已掌握的题目 |
| S-08 | 在线编程 | C++ 在线编译运行（C++17 标准） |
| S-09 | 学习仪表盘 | 学习进度、成绩统计、能力维度可视化 |
| S-10 | 全局搜索 | 搜索对话记录、课程资料、错题内容 |
| S-11 | 忘记密码 | 通过邮箱重置密码 |

#### 3.1.2 教师端需求

| 编号 | 功能 | 描述 |
|------|------|------|
| T-01 | 注册/登录 | 教师注册账号并登录系统 |
| T-02 | 资料管理 | 上传课件（PDF/PPT/Word/MD/TXT），自动解析文本，构建知识库 |
| T-03 | 考核配置 | 配置章节考核的题型数量、知识点、评价维度、时间限制 |
| T-04 | 学生管理 | 查看学生列表、编辑信息、重置密码、批量导入（CSV/Excel） |
| T-05 | 成绩查看 | 查看班级成绩、逐题查看学生答卷 |
| T-06 | 模型配置 | 运行时配置 LLM API Key、Base URL、Model |
| T-07 | 教学仪表盘 | 学生统计、成绩分布、章节通过率 |

### 3.2 非功能需求

| 编号 | 类别 | 要求 |
|------|------|------|
| NF-01 | 性能 | API 响应时间 < 2s（不含 LLM 调用），SSE 流式输出延迟 < 500ms |
| NF-02 | 并发 | 支持 50+ 用户同时在线 |
| NF-03 | 安全 | JWT 认证，密码 bcrypt 哈希，API Key 脱敏 |
| NF-04 | 可部署 | 支持 Docker 一键部署 |
| NF-05 | 浏览器兼容 | 支持 Chrome、Edge、Firefox 最新版本 |

---

## 4 系统设计

### 4.1 系统架构

系统采用前后端分离的三层架构：

```
┌────────────────────────────────────────────────────────┐
│                      用户层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 学生客户端 │  │ 教师客户端 │  │ 管理员    │              │
│  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘              │
│        │              │              │                    │
├────────┼──────────────┼──────────────┼────────────────────┤
│        │        Nginx 反向代理         │     前端部署层      │
│        └──────────────┼──────────────┘                    │
│                       │                                   │
├───────────────────────┼───────────────────────────────────┤
│                       ▼                   后端服务层       │
│  ┌──────────────────────────────────────┐                 │
│  │          FastAPI (Uvicorn)           │                 │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌───────┐ │                 │
│  │  │Auth │ │Chat │ │Exam │ │Search │ │ ← 7 个路由模块    │
│  │  └─────┘ └─────┘ └─────┘ └───────┘ │                 │
│  │  ┌─────────────┐ ┌───────────────┐  │                 │
│  │  │ RAG 检索服务  │ │ LLM 调用服务   │  │                 │
│  │  └─────────────┘ └───────────────┘  │                 │
│  └──────────────────────────────────────┘                 │
│                       │                                   │
├───────────────────────┼───────────────────────────────────┤
│                       ▼                   数据层          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │  SQLite  │  │ Chroma   │  │  文件系统  │               │
│  │  数据库   │  │ 向量存储  │  │  (Uploads) │               │
│  └──────────┘  └──────────┘  └──────────┘               │
└────────────────────────────────────────────────────────┘
```

### 4.2 前后端分离设计

前端 Vue 3 应用通过 HTTP 请求与后端 FastAPI 通信。开发环境下 Vite Dev Server 将 `/api` 请求代理到后端 8000 端口。生产环境下 Nginx 作为反向代理，将 `/api/` 和 `/uploads/` 请求转发到后端容器。

### 4.3 项目目录结构

```
cpp-agent-system/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # 路由层：7 个模块，50 个端点
│   │   ├── core/            # 配置、安全、数据库连接
│   │   ├── models/          # 数据模型：8 张表
│   │   ├── schemas/         # Pydantic 请求/响应模型
│   │   ├── services/        # 业务逻辑：LLM、考试、C++运行、RAG
│   │   └── utils/           # 工具：文件解析
│   ├── Dockerfile
│   ├── requirements.txt
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── views/           # 19 个页面组件
│   │   ├── components/      # 公共组件：侧边栏、搜索栏、聊天组件
│   │   ├── api/             # API 调用封装
│   │   ├── stores/          # Pinia 状态管理
│   │   ├── router/          # Vue Router 5 配置
│   │   └── assets/          # 全局样式
│   ├── Dockerfile
│   ├── nginx.conf
│   └── vite.config.js
├── docker-compose.yml
└── README.md
```

---

## 5 功能说明

### 5.1 认证与权限管理

系统实现基于 JWT 的双角色认证体系。用户注册时选择"学生"或"教师"角色，登录后后端签发 JWT Token。前端路由守卫通过 Pinia Store 中的用户角色控制页面访问权限。关键安全措施包括：密码使用 bcrypt 加盐哈希存储，JWT 配置过期时间（默认 30 分钟），API 端点通过依赖注入验证身份和角色。

### 5.2 AI 智能对话

对话功能是本系统的核心模块，工作流程如下：

1. 学生输入问题 → 前端通过 Fetch API 发起 POST 请求
2. 后端 RAG 检索器从知识库中匹配相关内容（中文双字词匹配 + 磁盘缓存）
3. 构建包含课程资料的 System Prompt，调用 DeepSeek API 流式输出
4. 前端通过 SSE（Server-Sent Events）逐字渲染回答
5. 解析回答末尾的 `[FOLLOWUP]` 标记，展示 2-3 个追问建议
6. 对话内容保存到 `chat_logs` 表，支持历史查看和删除

### 5.3 资料管理与知识库

教师上传课件时，系统自动根据文件扩展名选择解析器：
- PDF → PyPDF2 提取文本
- PPTX → python-pptx 提取文本
- DOCX → python-docx 提取文本
- MD/TXT/CPP → 多编码尝试读取

解析后的文本按段落切分为 chunks，通过关键词匹配算法建立检索索引，持久化到磁盘缓存文件中。RAG 检索时对查询进行中文双字词分词，与 chunks 进行匹配排序。

### 5.4 章节考核

教师配置考核参数后，学生点击开始考核，系统调用 LLM 按照配置的题型比例生成题目。答题过程中自动保存进度，支持中断恢复。提交后 LLM 逐题批改，从"知识掌握情况""基础概念理解""综合分析能力"三个维度生成评价报告（含分数、维度评分、复习建议、综合评价）。错题自动记录到错题本。

### 5.5 在线编程运行

学生输入 C++ 代码，后端将代码写入临时文件，调用 g++（C++17 标准）编译，编译成功后运行可执行文件（5 秒超时保护），返回标准输出和标准错误。支持通过 stdin 传入输入数据。

### 5.6 全局搜索

支持跨模块关键词搜索：对话记录（问题和回答）、课程资料（文件名和解析内容）、错题本（题目文本）、学生列表（仅教师可见）。使用 SQL LIKE 模糊匹配，支持分页。

---

## 6 数据结构设计

### 6.1 数据库 ER 图

```
┌──────────┐       ┌──────────────────┐       ┌──────────────┐
│   User   │──────▶│ TeacherStudent   │◀──────│    User      │
│ (student)│       │ (teacher_id,      │       │  (teacher)   │
│          │       │  student_id)      │       │              │
└────┬─────┘       └──────────────────┘       └──────┬───────┘
     │                                               │
     │  ┌─────────────────┐                          │
     ├─▶│   ExamRecord    │◀─ ┌──────────────┐      │
     │  │ (student_id,     │   │  ExamConfig  │◀─────┤
     │  │  exam_config_id) │   │ (teacher_id) │      │
     │  └────────┬────────┘   └──────┬───────┘      │
     │           │                    │               │
     │  ┌────────┴────────┐  ┌───────┴───────┐      │
     ├─▶│  WrongAnswer    │  │   Chapter    │      │
     │  │ (exam_record_id)│  └──────────────┘      │
     │  └─────────────────┘                         │
     │  ┌─────────────────┐                         │
     ├─▶│    ChatLog      │    ┌──────────────────┐ │
     │  │   (user_id)     │    │ CourseMaterial   │◀┤
     │  └─────────────────┘    │  (teacher_id,    │ │
     │                         │   chapter_id)    │ │
     │                         └──────────────────┘ │
     └──────────────────────────────────────────────┘
```

### 6.2 数据表结构

#### users — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password_hash | VARCHAR(255) | bcrypt 密码哈希 |
| role | VARCHAR(10) | 角色：student / teacher |
| full_name | VARCHAR(50) | 真实姓名 |
| email | VARCHAR(100) | 邮箱（用于密码重置） |
| created_at | DATETIME | 注册时间 |

#### chapters — 课程章节表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| title | VARCHAR(200) | 章节标题 |
| description | TEXT | 章节描述 |
| order | INTEGER | 排序序号 |
| course_name | VARCHAR(100) | 课程名称 |

#### course_materials — 课件资料表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| teacher_id | INTEGER FK | 上传教师 |
| chapter_id | INTEGER FK | 所属章节 |
| file_name | VARCHAR(255) | 原始文件名 |
| file_path | VARCHAR(255) | 存储路径 |
| file_type | VARCHAR(10) | 文件扩展名 |
| parsed_content | TEXT | 解析后的文本内容 |
| uploaded_at | DATETIME | 上传时间 |

#### exam_configs — 考核配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| chapter_id | INTEGER FK | 考核章节 |
| teacher_id | INTEGER FK | 配置教师 |
| total_questions | INTEGER | 总题数 |
| choice_count | INTEGER | 选择题数量 |
| truefalse_count | INTEGER | 判断题数量 |
| essay_count | INTEGER | 简答题数量 |
| programming_count | INTEGER | 编程题数量 |
| knowledge_points | TEXT (JSON) | 知识点列表 |
| evaluation_dimensions | TEXT (JSON) | 评价维度列表 |
| time_limit_minutes | INTEGER | 时间限制（分钟） |
| created_at | DATETIME | 创建时间 |

#### exam_records — 考试记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| student_id | INTEGER FK | 考生 |
| exam_config_id | INTEGER FK | 考核配置 |
| answers | TEXT (JSON) | 题目和答案（JSON） |
| score | REAL | 得分 |
| dimensions_scores | TEXT (JSON) | 各维度评分 |
| report_text | TEXT | 综合评价文本 |
| started_at | DATETIME | 开始时间 |
| completed_at | DATETIME | 完成时间 |
| status | VARCHAR(20) | 状态：in_progress / completed |

#### chat_logs — 对话日志表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| user_id | INTEGER FK | 用户 |
| question | TEXT | 用户问题 |
| answer | TEXT | AI 回答 |
| rag_sources | TEXT (JSON) | RAG 检索来源 |
| recommended_resources | TEXT (JSON) | 推荐学习资源 |
| created_at | DATETIME | 对话时间 |

#### teacher_students — 师生关系表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| teacher_id | INTEGER FK | 教师 ID |
| student_id | INTEGER FK | 学生 ID |
| status | VARCHAR(10) | 状态：active / inactive |
| created_at | DATETIME | 建立时间 |

#### wrong_answers — 错题本表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| student_id | INTEGER FK | 学生 |
| exam_record_id | INTEGER FK | 考试记录 |
| chapter_title | VARCHAR(200) | 章节标题 |
| question_type | VARCHAR(20) | 题型 |
| question_text | TEXT | 题目内容 |
| correct_answer | TEXT | 正确答案 |
| student_answer | TEXT | 学生答案 |
| created_at | DATETIME | 记录时间 |

---

## 7 API 接口文档

系统共 50 个 API 端点，挂载在 `/api/v1` 前缀。完整文档可通过 Swagger UI 访问：`http://localhost:8000/docs`

### 7.1 认证模块 `/api/v1/auth`（6 个端点）

| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| POST | /auth/register | 用户注册 | 无 |
| POST | /auth/login | 用户登录，返回 JWT | 无 |
| GET | /auth/me | 获取当前用户信息 | JWT |
| PUT | /auth/profile | 修改用户名/姓名/密码 | JWT |
| POST | /auth/forgot-password | 发送密码重置邮件 | 无 |
| POST | /auth/reset-password | 通过 token 重置密码 | 无 |

### 7.2 教师模块 `/api/v1/teacher`（18 个端点）

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /teacher/materials/upload | 上传课件资料 |
| GET | /teacher/materials | 列出已上传资料（分页） |
| DELETE | /teacher/materials/{id} | 删除单个资料 |
| POST | /teacher/materials/batch-delete | 批量删除资料 |
| POST | /teacher/exams | 创建考核配置 |
| GET | /teacher/exams | 列出考核配置（分页） |
| DELETE | /teacher/exams/{id} | 删除考核配置 |
| GET | /teacher/exams/{id}/results | 查看某考核学生成绩 |
| GET | /teacher/exams/{eid}/student/{sid}/answers | 查看学生答卷详情 |
| GET | /teacher/students | 列出学生（分页） |
| GET | /teacher/students/{id}/exams | 查看学生考试记录 |
| PUT | /teacher/students/{id} | 修改学生信息 |
| POST | /teacher/students/{id}/reset-password | 重置学生密码 |
| DELETE | /teacher/students/{id} | 移除学生 |
| POST | /teacher/students/import | 批量导入学生 |
| GET | /teacher/chapters | 获取章节列表 |
| GET | /teacher/model-config | 查看 LLM 配置 |
| PUT | /teacher/model-config | 更新 LLM 配置 |
| GET | /teacher/stats | 教学仪表盘统计 |

### 7.3 学生模块 `/api/v1/student`（15 个端点）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /student/materials | 浏览课程资料（分页） |
| GET | /student/materials/{id}/download | 下载/查看资料文件 |
| GET | /student/exams | 查看可用考核列表 |
| POST | /student/exams/{id}/start | 开始考核（LLM 出题） |
| POST | /student/exams/{id}/answer | 提交单题答案 |
| POST | /student/exams/{id}/submit | 交卷并触发批改 |
| GET | /student/exams/{id}/report | 查看评价报告 |
| GET | /student/exams/history | 考核历史记录 |
| GET | /student/exams/{id}/export | 导出报告为 HTML |
| GET | /student/wrong-answers | 查看错题本 |
| DELETE | /student/wrong-answers/{id} | 删除错题 |
| POST | /student/cpp-run | C++ 在线编译运行 |
| GET | /student/teachers | 查看可选教师 |
| GET | /student/teacher | 查看当前教师 |
| POST | /student/teacher | 选择/更换教师 |

### 7.4 对话模块 `/api/v1/chat`（6 个端点）

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /chat/stream | SSE 流式对话（RAG + 资源推荐） |
| GET | /chat/conversations | 对话列表（分页） |
| GET | /chat/history | 对话历史（分页） |
| GET | /chat/history/{id} | 获取单条对话完整内容 |
| DELETE | /chat/history/{id} | 删除单条对话 |
| DELETE | /chat/history | 清空全部对话 |

### 7.5 其他模块

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /dashboard/student | 学生学习仪表盘 |
| GET | /dashboard/teacher | 教师教学仪表盘 |
| GET | /search?q=&scope=&page=&page_size= | 全局搜索 |
| GET | /admin/stats | 系统统计 |
| GET | /admin/logs | 系统日志 |

---

## 结论与展望

本项目成功实现了一套功能完备的 C++ 课程智能体系统，整合了大语言模型、RAG 检索增强、在线考核评估、学习数据分析等多项技术能力。系统支持教师和学生双角色，覆盖了课件上传、知识库构建、AI 对话、自动出题批改、学习仪表盘等完整教学闭环。

### 技术成果

- 后端实现 50 个 API 端点，数据库包含 8 张核心表
- 前端实现 19 个页面组件，覆盖全部功能场景
- 支持 Docker 一键部署，Nginx 反向代理
- RAG 知识库持久化，累计可检索 1600+ 文本片段
- C++ 在线编译运行支持 C++17 标准

### 改进方向

1. **向量检索升级**：当前 RAG 使用关键词匹配，未来可切换为 Chroma 向量数据库 + Embedding 语义检索，提升中文检索准确率。
2. **邮件服务集成**：忘记密码功能目前为开发模式，生产环境需配置 SMTP 邮件发送。
3. **管理员后台**：后端已有 `/admin` 端点，前端尚未实现管理员可视化面板。
4. **WebSocket 实时通信**：可将 SSE 升级为 WebSocket，支持双向实时交互。
5. **代码相似度检测**：增加编程题查重功能，防止学生抄袭代码。
6. **多课程支持**：当前为 C++ 课程定制，可通过配置扩展为通用教学平台。
